#include <stdio.h>
#include <string.h>

int main(){
    

}

    // int size =5;
    // int ar[size] = {4,6,7,3,2};
    // // int big=0,small=ar[0];
    // for (int r=size;r>=0;r++){
    //     if (ar[r]>big) big = ar[r];
    //     else if (ar[r]<small) small = ar[r];
    // }
    // printf("%d %d",big,small);

    /*
    int temp,size = 5,ar[size]={3,4,5,1,0};
    for (int r=0;r<(size/2);++r){
        temp = ar[r];
        ar[r]=ar[size-r-1];
        ar[size-r-1]=temp;
    }
    

    for(int i = 0; i < size; i++)
        printf("%d ", ar[i]);
    return 0;
    */
